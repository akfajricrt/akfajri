---
title: 2. Install Stack LEMP
sidebar_label: 2. Install LEMP
---

Kita akan menginstal Nginx, MariaDB, dan PHP.

### 1. Install Nginx & MariaDB
```bash
sudo apt install -y nginx mariadb-server
sudo apt install -y php-fpm php-mysql php-xml php-mbstring php-curl php-zip php-gd php-intl
